

https://www.freecodecamp.org/news/how-to-fix-git-always-asking-for-user-credentials/

update the URL of origin remote using SSH instead of HTTPS;

git remote set-url origin git@github.com:username/repo.git

git remote set-url origin git@github.com:williamszk/vim-tmux-config.git

https://www.ssh.com/academy/ssh/keygen

ssh-keygen

this is a better and easier method
we can try to git clone using the ssh
then we need to create a key pair inside the container
then we need to cat the public key and then
we need to include it inside the github account

